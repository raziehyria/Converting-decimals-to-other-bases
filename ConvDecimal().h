#include <stdio.h>

// binary converter header function
int ConvDecimal(int num)
{
    // creating an array to store the numbers (max 7 characters)
    int binNum[32];
    // used to keep track of the position we are inserting into
    int index=7; 
    
    // exit condition for recursive loop
    if(num == 0)
        {
            return 0;
        }
    // recurse and append remainder into array
    else
        {
         // while the number is greater than 0   
        for(num > 0);
            {

            }

        }
    
}
